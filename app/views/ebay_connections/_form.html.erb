<%= form_for(@ebay_connection) do |f| %>
  <% if @ebay_connection.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ebay_connection.errors.count, "error") %> prohibited this ebay_connection from being saved:</h2>

      <ul>
      <% @ebay_connection.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
